<?php

declare(strict_types=1);

/**
 * English copy for the Pageflow plugin's user-facing messages.
 *
 * Pageflow is transport, not a domain, so it has very little to say to a person
 * — but what it does say reaches one: this is the body a streamed page returns
 * when the viewer is not signed in.
 */
return [
    'stream' => [
        'unauthenticated' => 'Authentication is required.',
    ],
];
